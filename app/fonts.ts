import { Open_Sans, Squada_One } from 'next/font/google';

export const openSans = Open_Sans({
  subsets: ['latin'],
  weight: ['400', '600'],
  display: 'swap',
});

export const squadaOne = Squada_One({
  subsets: ['latin'],
  weight: "400",
});